mutation claimTranscriptionResult($jobId: String!) {
  claimTranscriptionResult(jobId: $jobId) {
    status
    transcription {
      speaker
      start
      end
      transcription
    }
    summary
  }
}
