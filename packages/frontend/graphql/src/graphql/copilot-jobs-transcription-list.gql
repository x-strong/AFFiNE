query getTranscriptionJobs(
  $workspaceId: String!
) {
  currentUser {
    copilot(workspaceId: $workspaceId) {
      transcriptionsJobs {
        id
        workspaceId
        blobId
        createdBy
        status
      }
    }
  }
}
